STORE CCCMSGCK_S_MSG
SELECT 'X' 
FROM PS_PAY_MESSAGE 
WHERE RUN_ID = :1 
  AND OFF_CYCLE='N'
;

STORE CCCMSGCK_S_MSGOFF
SELECT 'X' 
FROM PS_PAY_MESSAGE 
WHERE COMPANY=:1 
  AND PAYGROUP = :2 
  AND PAY_END_DT = :3 
  AND PAGE_NUM BETWEEN :4 
  AND :5 
  AND OFF_CYCLE='Y'
;
